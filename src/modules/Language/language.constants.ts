import { KeysArray } from '#types';
import { LanguageSelect } from './language.types';
export const LANGUAGE_SELECT: KeysArray<LanguageSelect> = [
    'id',
    'name',
    'slug',
    'quizIds',
    'quiz',
];
